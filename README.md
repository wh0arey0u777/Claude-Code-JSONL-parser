# Claude-Code-JSONL-parser
A command-line utility for parsing complex JSON Lines (JSONL) log files into structured Excel spreadsheets (.xlsx). Designed to extract specific, critical fields from unstructured system logs, making large datasets immediately usable for data analysis and reporting. 
